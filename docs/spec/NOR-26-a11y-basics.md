# NOR-26 — 404와 접근성 기본기

## 범위

- 모든 페이지 공통 레이아웃에서 본문으로 바로 이동하는 skip-link를 제공한다.
- 모든 실제 라우트의 유일한 `main`에 `main-content` 식별자를 둔다.
- 404는 설명적인 제목·본문·복구 링크를 가진 정적 HTML로 출력하고 검색 색인을 막는다.
- 키보드 포커스는 전역 CSS에서 항상 보인다.

## 비범위

자동 axe/Lighthouse 감사와 성능 예산은 NOR-34에서 처리한다.
