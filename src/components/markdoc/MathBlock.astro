---
import katex from 'katex';

interface Props {
  formula: string;
}

const { formula } = Astro.props;
const html = katex.renderToString(formula, {
  throwOnError: false,
  displayMode: true,
  output: 'htmlAndMathml',
});
---

<div class="katex-block" set:html={html} />
