---
interface Props {
  src: string;
  alt: string;
  title?: string;
}

const { src, alt, title } = Astro.props;
---

<img
  src={src}
  alt={alt}
  title={title}
  loading="lazy"
  decoding="async"
  style="max-width: 100%; height: auto;"
/>
